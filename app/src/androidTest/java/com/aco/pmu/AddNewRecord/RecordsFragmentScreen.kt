package com.aco.pmu.AddNewRecord

import com.agoda.kakao.screen.Screen
import com.agoda.kakao.text.KButton
import com.aco.pmu.R

object RecordsFragmentScreen : Screen<RecordsFragmentScreen> () {

   // val toolbar = KView { withId(R.id.toolbar)}

  //  val title = KView { withText("Записи") }

    val addNewRecordButton = KButton { withId(R.id.floatingActionButton)}

  //  val calendar = KView { withId(R.id.calendarView1) }

  //  val recyclerView = KView { R.id.recycler_view }

}